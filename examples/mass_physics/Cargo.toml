[package]
name = "mass_physics"
version = "0.1.0"
edition = "2021"

[features]
default = []
gpu-physics = ["rein/gpu-physics", "rein/compute"]

[dependencies]
rein = { path = "../..", features = ["engine", "physics"] }
glam = "0.31"
anyhow = "1"
hecs = "0.10"
